name: Update badges

on:
  push:
    branches:
      -main
  #schedule:
    # Runs at 12am UTC
    #- cron: "0 12 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@main

